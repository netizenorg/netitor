the `nn-canvas.json` and `nn-dom-element.json` were created using an LLM (which parsed through the relevant nn source files and docs to automate the process) and are only being used to create code hinting lists right now. b/c of the way the dbl-click edu-info works, i haven't found away to effectively present this as edu data when dbl-clicking (sometimes a method is being called on a variable which is an instance of an nn DOM element... hard to find given the current logic, unless we do something similar to the `evaluate` method of the `jsHinter.js` ...might come back to this later)
