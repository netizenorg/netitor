// const CodeMirror = require('codemirror')
// const CSSMode = CodeMirror.resolveMode('text/css')
// console.log(JSON.stringify(CSSMode))
// console.log(Object.keys(CSSMode.propertyKeywords).length) // 426

// const { CSSLint } = require('csslint') // TBD

function linter (code) {
  // const results = CSSLint.verify('.test { color: red; }')
  // console.log(results)
  return null
}

module.exports = linter
